# Copy to .env and fill in. For Postgres, set DATABASE_URL to a Postgres URL
# and change the provider in prisma/schema.prisma to "postgresql".
DATABASE_URL="file:./dev.db"

# Generate with: openssl rand -base64 32
SESSION_SECRET=""
